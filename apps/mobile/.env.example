# Public values only. react-native-config compiles this file into the app.
# No secrets belong here: no auth tokens, keystore passwords, or private keys.
#
# Copy to the gitignored files next to this one:
#   cp .env.example .env.development
#   cp .env.example .env.staging
#   cp .env.example .env.production
# Scripts select one with ENVFILE (see package.json).

GRAPHQL_URL=http://localhost:4000/graphql
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=1
