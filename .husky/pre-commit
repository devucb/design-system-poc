#!/bin/sh
if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is not installed." >&2
  echo "Install it (brew install gitleaks), then: npx husky" >&2
  exit 1
fi

gitleaks protect --staged --redact --no-banner
